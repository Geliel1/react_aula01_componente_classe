import React from 'react';
import ReactDOM from 'react-dom';
import OlaMundo from './components/index'

ReactDOM.render(
  <React.StrictMode>
    <OlaMundo />
  </React.StrictMode>,
  document.getElementById('root')
);